# Claim 4 method

The verifier hash-checks paper and authors-code excerpts, then requires all
Equation-18, `argmax_phi`, adversarial-scheme, and `optimizerD.step()` markers.

An independent complete finite checker uses `mu=delta_1`, `nu=delta_0`,
`T_t(1)=t`, `phi in [0,1]`, and the PSD kernel
`k_phi(x,y)=(phi x)(phi y)`. Equation (18) becomes

`max_phi min_t 0.5(1-t)^2 + phi^2 t^2`.

The analytic and exhaustive 1001-point solutions agree:
`phi*=1`, `t*=1/3`. Suppressing discriminator optimization by fixing
`phi=0` gives `t=1`, shifting the generator solution by `2/3`.

The negative control substitutes the true fixed-kernel Algorithm-1 statement.
The completeness gate rejects it because it contains none of Equation 18,
`max_phi`, or `argmax_phi`, and the standalone verifier exits 1.

Fixed cumulative command: `uv run --frozen python run_campaign.py`.
