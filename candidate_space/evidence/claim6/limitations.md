# Limitations

The general argument is an independently reconstructed symbolic derivation,
not a proof-assistant formalization. The numerical checker is a continuous
nonlinear two-parameter family rather than a neural network. Its metric is
non-identity, varying, positive definite, and off-diagonal.
